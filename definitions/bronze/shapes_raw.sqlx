config {
    type: 'incremental',
    schema: 'bronze',
    name: 'shapes_raw',
    tags: ['shapes'],
    protected: true
}

js {
    const {
        delete_statement,
        select_statement
    } = functions_bronze.buildDesertBronze('shapes');
}

SELECT
  *
FROM
  TEMP

pre_operations {
  CREATE TEMP TABLE TEMP AS ${select_statement}
    ${
        when(
            incremental(),
            delete_statement
        )
    }
}
