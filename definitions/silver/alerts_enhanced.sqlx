config {
    type: 'incremental',
    schema: 'silver',
    name: 'alerts_enhanced',
    dependencies: [
        'alerts_raw'
    ],
    tags: ['mbta']
}

js {
    const {
        deleteStatement,
        selectStatement
    } = functions.desertSilver();
}

SELECT
  *
FROM
  TEMP

pre_operations {
  CREATE TEMP TABLE TEMP AS ${selectStatement}
    ${
        when(
            incremental(),
            deleteStatement
        )
    }
}
