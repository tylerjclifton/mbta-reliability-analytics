config {
    type: 'incremental',
    schema: 'stops',
    name: 'stops_silver',
    tags: ['stops'],
    dependencies: ['stops_bronze']
}

js {
    const {
        delete_statement,
        select_statement
    } = functions.buildDesertSilver('stops');
}

SELECT
  *
FROM
  TEMP

pre_operations {
  CREATE TEMP TABLE TEMP AS ${select_statement}
    ${
        when(
            incremental(),
            delete_statement
        )
    }
}
