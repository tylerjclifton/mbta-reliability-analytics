config {
    type: 'incremental',
    schema: 'mbta',
    name: 'bronze_alerts',
    tags: ['mbta'],
    protected: true,
    hermetic: true
}

js {
    const {
        delete_statement,
        select_statement
    } = functions.buildDesertBronze('alerts');
}

SELECT
  *
FROM
  TEMP

pre_operations {
  CREATE TEMP TABLE TEMP AS ${select_statement}
    ${
        when(
            incremental(),
            delete_statement
        )
    }
}
