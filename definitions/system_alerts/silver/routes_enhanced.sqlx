config {
    type: 'incremental',
    schema: 'silver',
    name: 'routes_enhanced',
    dependencies: [
        'alerts_raw',
        'routes_raw'
    ],
    tags: ['mbta']
}

js {
    const {
        deleteStatement,
        selectStatement
    } = functions.desertSilver();
}

SELECT
  *
FROM
  TEMP

pre_operations {
  CREATE TEMP TABLE TEMP AS ${selectStatement}
    ${
        when(
            incremental(),
            deleteStatement
        )
    }
}
