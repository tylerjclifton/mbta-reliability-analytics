config {
    type: 'incremental',
    schema: 'alerts',
    name: 'alerts_bronze',
    tags: ['alerts'],
    protected: true
}

js {
    const {
        delete_statement,
        select_statement
    } = functions.buildDesertBronze('alerts');
}

SELECT
  *
FROM
  TEMP

pre_operations {
  CREATE TEMP TABLE TEMP AS ${select_statement}
    ${
        when(
            incremental(),
            delete_statement
        )
    }
}
