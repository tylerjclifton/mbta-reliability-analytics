config {
    type: 'incremental',
    schema: 'bronze',
    name: 'stops_raw',
    tags: ['stops'],
    protected: true
}

js {
    const {
        delete_statement,
        select_statement
    } = functions_bronze.buildDesertBronze('stops');
}

SELECT
  *
FROM
  TEMP

pre_operations {
  CREATE TEMP TABLE TEMP AS ${select_statement}
    ${
        when(
            incremental(),
            delete_statement
        )
    }
}
