config {
    type: 'incremental',
    schema: 'stops',
    name: 'silver',
    tags: ['stops'],
    dependencies: [{
        schema: 'stops',
        name: 'bronze'
    }]
}

js {
    const {
        delete_statement,
        select_statement
    } = functions.buildDesertSilver('stops');
}

SELECT
  *
FROM
  TEMP

pre_operations {
  CREATE TEMP TABLE TEMP AS ${select_statement}
    ${
        when(
            incremental(),
            delete_statement
        )
    }
}
