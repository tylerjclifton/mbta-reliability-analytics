config {
    type: 'incremental',
    schema: 'shapes',
    name: 'bronze',
    tags: ['shapes'],
    protected: true
}

js {
    const {
        delete_statement,
        select_statement
    } = functions.buildDesertBronze('shapes');
}

SELECT
  *
FROM
  TEMP

pre_operations {
  CREATE TEMP TABLE TEMP AS ${select_statement}
    ${
        when(
            incremental(),
            delete_statement
        )
    }
}
