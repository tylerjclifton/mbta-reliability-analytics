config {
    type: 'incremental',
    dataset: 'mbta',
    name: 'silver_alerts',
    tags: ['alerts'],
    dependencies: [{
        dataset: 'alerts',
        name: 'bronze'
    }]
}

js {
    const {
        delete_statement,
        select_statement
    } = functions.buildDesertSilver('alerts');
}

SELECT
  *
FROM
  TEMP

pre_operations {
  CREATE TEMP TABLE TEMP AS ${select_statement}
    ${
        when(
            incremental(),
            delete_statement
        )
    }
}
