config {
    type: 'incremental',
    schema: 'bronze',
    name: 'routes_raw',
    tags: ['mbta'],
    protected: true
}

js {
    const {
        deleteStatement,
        selectStatement
    } = functions.desertBronze('routes');
}

SELECT
  *
FROM
  TEMP

pre_operations {
  CREATE TEMP TABLE TEMP AS ${selectStatement}
    ${
        when(
            incremental(),
            deleteStatement
        )
    }
}
